# Format repository with Prettier

Apply consistent formatting across the entire codebase.

## Command

```bash
npm run format
```

## Git Status Summary

After formatting, show which files changed:

```bash
git status --porcelain
```

## Output

Print a brief summary of:

- Number of files formatted
- Types of files affected (JS, JSON, MD, etc.)
- Any files that couldn't be formatted
